class Surgeon {
  constructor(name, department) {
    this.name = name;
    this.department = department;
  }
}

const surgeonCurry = new Surgeon('Curry','Cardiovascular') // Create new Dog instance

const surgeonDurant = new Surgeon('Durant','Orthopedics') // Create new Dog instance
